# Repo Instructions

- Simplicity first. Minimal code changes.
- Next.js app with Canvas 2D animation engine in `src/lib/engine.ts`.
- Use `npm run dev` to start dev server.
- American English everywhere.
